{% extends 'base.html.twig' %}

{% block title %}Interface Restaurateur{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Vous Etes sur l'interface Restaurateur !</h1>
<ul>
</div>
    <table class="table">
        <thead>
            <tr>
                <th>nom du restaurant</th>
                <th>adresse du restaurant</th>
                <th>type du restaurant</th>
                <th>nom du restauranteurs</th>
                <th>voir disponile</th>
            </tr>
        </thead>
   <tbody>
        {% for restaurateur in restaurateurs %}
        {% for typeplat in typeplats %}
        {% for user in users %}
            <tr>
                <td>{{restaurateur.nom}}</td>
                <td>{{restaurateur.adresse}}</td>
                <td>{{typeplat.libelle}}</td>
                <td>{{user.nom}}</td>
                <td><a href="{{ path('app_disponible', {'id': restaurateur.id}) }}">voir disponile</a></td>
        {% endfor %}
        {% endfor %}
        {% endfor %}
        </tr>
        
     </tbody>
    </table>
{% endblock %}